on:
  push:
    branches: [build-dockerfile]
    paths:
      - 'Dockerfile'

name: build-magpie-image

jobs:
  build:
    if: github.repository == 'pascal-sauer/magpiemodel'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/pascal-sauer/magpie-image:latest
            ghcr.io/pascal-sauer/magpie-image:${{ github.sha }}
